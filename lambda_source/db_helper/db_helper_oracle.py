import oracledb

def get_current_users_and_roles_oracle(db_host, db_name, db_user, db_password):
    '''
    return a list of users and roles for the given oracle db
    '''
    #TODO add function
    print(f"get current users and roles for oracle")
