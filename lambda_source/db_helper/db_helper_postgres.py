import psycopg2

def get_current_users_and_roles_postgres(db_host, db_name, db_user, db_password):
    '''
    return a list of users and roles for the given postgres db
    '''
    #TODO add function
    print(f"get current users and roles for postgres")